{
  "name": "delo-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Дело — выгрузи дела из головы",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "refresh": "vite build && vite preview",
    "sync": "npm run build && npx cap sync",
    "cap:sync": "npm run build && npx cap sync",
    "release": "npm run build && npx cap sync",
    "cap:android": "npx cap open android",
    "cap:ios": "npx cap open ios",
    "android:apk": "npm run sync && npx cap run android",
    "splash:square": "node scripts/make-square-splash.js",
    "icons:pwa": "node scripts/make-pwa-icons.js",
    "icons:ios": "node scripts/make-ios-icon.js",
    "icons:android": "node scripts/make-android-mipmap-icons.js",
    "icons:all": "node scripts/make-square-splash.js && node scripts/make-android-mipmap-icons.js && node scripts/make-ios-icon.js && node scripts/make-pwa-icons.js",
    "store-assets": "node scripts/make-store-assets.js"
  },
  "dependencies": {
    "@capacitor/app": "^8.0.0",
    "@capacitor/core": "^8.0.2",
    "@capacitor/local-notifications": "^8.0.0",
    "@capawesome/capacitor-badge": "^8.0.0",
    "@dnd-kit/core": "^6.3.1",
    "@dnd-kit/sortable": "^10.0.0",
    "@dnd-kit/utilities": "^3.2.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/cli": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@vitejs/plugin-react": "^4.2.1",
    "sharp": "^0.33.5",
    "vite": "^5.0.0"
  }
}
